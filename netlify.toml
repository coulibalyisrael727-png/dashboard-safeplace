[build]
  command = "python manage.py collectstatic --noinput && echo 'Static files collected'"
  publish = "staticfiles"

[[redirects]]
  from = "/api/*"
  to = "https://{env:MAIN_API_URL}/api/:splat"
  status = 200
  force = true

# Optional headers for CORS (Netlify adds automatically for redirects)